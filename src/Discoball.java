import processing.core.PImage;

import java.util.List;

public class Discoball extends Animated {
    public Discoball(String name, Point position, List<PImage> pImages, int rate, int animationRate) {
        super(name,position,pImages,rate,animationRate);
    }

}