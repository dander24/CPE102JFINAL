import processing.core.PImage;

import java.util.List;

public class DiscoMiner extends Miner {

    public DiscoMiner(String name, Point position, int rate, int ResourceLimit, int AnimationRate, List<PImage> pImages) {
        super(name, position, rate, ResourceLimit, AnimationRate, pImages);
    }
}
